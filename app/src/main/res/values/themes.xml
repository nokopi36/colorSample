<resources>
    <!--
      画面の中身は Compose (ui/theme/Theme.kt) が描くため、ここは Activity 起動から
      最初のフレームが出るまでの下地だけを決める最小の定義にしてある。
    -->
    <style name="Theme.ColorSample" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
