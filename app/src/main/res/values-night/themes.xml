<resources>
    <!-- 起動時の下地のみ。中身は Compose の ColorSampleTheme が描く。 -->
    <style name="Theme.ColorSample" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
